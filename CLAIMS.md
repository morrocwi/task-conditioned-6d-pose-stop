# Claim boundaries

| Claim | Status |
|---|---|
| Task declaration can change a stopping decision | Executed finite diagnostic |
| Coupled task constraints can invalidate independent-coordinate gates | Executed counterexample |
| Binary PASS/FAIL state can lose transition-relevant magnitude information | Executed counterexample |
| Dijkstra and pinned IDM min-plus core recover the same modeled finite optimum in the fixture | Executed finite diagnostic |
| IDM is faster than Dijkstra | Not supported |
| Task-conditioned stopping reduces real 6D pose latency | Open hypothesis |
| Task-conditioned stopping is non-inferior on real manipulation | Open hypothesis |
| Selective stopping is robust to uncertainty undercoverage | Refuted as a universal claim by the stress fixture |
| FoundationPose supports arbitrary coordinate-selective refinement unchanged | Not supported by current inspected interface |

No synthetic action cost in this repository should be reported as milliseconds, GPU time, energy, or physical robot work.
